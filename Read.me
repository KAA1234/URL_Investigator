https://developer.mozilla.org/en-US/docs/Learn/Server-side/Express_Nodejs

run by typing nodemon start into the terminal. This runs node ./bin/wwww

Adding routes and connecting to views:

    app.js (/, indexRouter)
    ^
    routes/index.js
        router.get (/, ....)
            res.render (index, {VAR})
    ^
    views/index.pug
    
To Do:  from the /submit-comments page it does not allow you to search for a new url
        Add GenAI comment to db